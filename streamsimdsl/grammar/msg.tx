import datatype

MessageModel:
    (
        (imports*=Import)
        (msgs*=Message)
    )#
;

Message: PubSubMessage;

PubSubMessage:
    'TopicMsg' name=ID
        ('type:' dataType=[datatype.DataType|FQN])?
        properties*=Property
    'end'
;